# Base URL for the LabVivoUC backend API (optional — falls back to mock/local data if unset)
NEXT_PUBLIC_LABVIVO_API_URL=

# FusionSolar (Huawei) credentials for the solar generation scraper — scraper-only, never exposed to the client
FUSIONSOLAR_EMAIL=
FUSIONSOLAR_PASSWORD=
